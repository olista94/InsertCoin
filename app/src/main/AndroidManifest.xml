<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.insertcoin">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".VariablesGlobales"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Juegos_ADD"></activity>
        <activity android:name=".Registro" />
        <activity android:name=".Comentarios_SHOWALL" />
        <activity android:name=".item_comentario" />
        <activity android:name=".activity_add_comentario" />
        <activity android:name=".Juegos_SHOWALL" />
        <activity android:name=".Juegos_SHOWCURRENT" />
        <activity android:name=".Usuarios_SHOWCURRENT" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>